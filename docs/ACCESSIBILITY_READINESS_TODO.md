# PJT-0001 · ACCESSIBILITY_READINESS_TODO

最后更新：2026-03-07  
当前复审结论：**NO**  
当前阶段判定：**可展示的无障碍原型 / 不足以作为真正可依赖的无障碍出行实验系统**

---

## 1. 本文档用途
这是一份**持续闭环待办文档**，用于记录：
- 当前系统距离“真正逻辑自洽且适合无障碍人士使用”还差什么。
- 接下来要逐步完成的改造项。
- 每完成一项就打勾并补充验证证据。
- 当所有任务完成后，重新问一遍：
  - **“这个系统是不是真的能够逻辑自洽且真的适合无障碍人士？”**

---

## 2. 闭环规则
1. 只有在**代码/数据/文档已落地 + 有验证证据**时，某项任务才允许打勾。
2. 每勾掉一项，必须同步更新：
   - `docs/ACCESSIBILITY_READINESS_TODO.md`
   - `docs/PROJECT_CHANGELOG.md`
3. 当本文件全部任务都打勾后，必须再次进行总复审：
   - 问题：**“这个系统是不是真的能够逻辑自洽且真的适合无障碍人士？”**
4. 如果复审答案是 **YES**：
   - 在 `PROJECT_CHANGELOG.md` 中记录结论；
   - 再删除本文件。
5. 如果复审答案是 **NO**：
   - 不删除本文件；
   - 追加新一轮问题、任务和验收项；
   - 继续下一轮闭环。

---

## 3. 当前情况摘要（2026-03-07）

### 3.1 已经具备的基础
- 已完成远端开发工作流整理：`8.134.206.52` 为主工作区，`8134` 为开发实例，`8133` 为生产实例。
- 前端已经从模板化展示改为较统一的无障碍视觉壳层。
- 已引入部分无障碍数据源与规则化前端自愈机制。
- 已具备基础的路线展示、地图展示、无障碍交互入口和自动化 UI 巡检能力。

### 3.2 当前仍然不能回答 YES 的核心原因
- 无障碍数据还不够完整、可信、可解释。
- 路径能力仍偏“公交查询 + 推荐”，还不是“从出门到到达”的全链路无障碍出行建模。
- 用户画像不足，无法真正区分视障、听障、轮椅/行动不便等不同人群的需求差异。
- 部分页面仍有稳定性和状态表达问题，存在“加载中/空白/弱提示”风险。
- 尚未形成小范围、人工核验过的真实试点数据。
- 缺少真实目标用户验证，当前更多是“开发者视角的无障碍”，不是“目标用户确认可用的无障碍”。

### 3.3 当前主要问题清单
1. **稳定性问题**：部分页面和状态仍可能出现持续加载、空态表达不足、错误原因不透明。
2. **可信度问题**：路线结果缺少数据来源、更新时间、置信度、风险提示。
3. **建模问题**：缺少起点步行段、站点入口/出口、换乘通道、电梯/坡道、目的地入口的完整建模。
4. **数据问题**：电梯、盲道、缘石坡道、站台可达性、目的地入口等关键数据覆盖不足。
5. **交互问题**：屏幕阅读器、听障场景文本提示、震动/视觉告警、焦点管理等尚未做完整基线验证。
6. **验证问题**：没有足够真实用户任务测试，无法证明“真的适合无障碍人士”。

---

## 4. 总体验收门槛（全部满足后才允许再问一次）
- 至少两个核心目标人群有明确支持策略，并且差异化逻辑已落地。
- 核心页面不存在持续加载、空白、遮挡、双滚动条、点击热区异常等关键可用性问题。
- 路线结果具备：推荐理由、风险说明、数据来源、更新时间、置信度。
- 关键数据缺失时，系统能够明确降级或拒绝推荐，而不是继续误导用户。
- 起点到终点的全链路关键段落（步行 / 上下车 / 换乘 / 到达入口）已有基本建模。
- 至少有一个小范围试点区域的数据经过人工核验。
- 至少完成一轮真实或准真实目标用户任务验证，并回写结论。

---

## 5. 待办清单（完成一项勾一项）

### A. 范围与目标用户冻结
- [x] A01 明确本项目首轮主服务人群与次服务人群，避免“所有残障群体都想覆盖”导致目标失焦。  
  完成标准：文档中明确写出主用户、次用户、暂不承诺覆盖的人群边界。  
  完成日期：2026-03-07  
  对应提交：`713575d`  
  验证方式：新增 `docs/TARGET_USER_SCOPE.md` 并更新 `PROJECT_RUNBOOK.md` / `PROJECT_CHANGELOG.md`，人工核对文档边界一致性  
  结果：通过
- [ ] A02 明确首轮试点范围（建议广州一个区或一组重点目的地），不再默认全城可用。  
  完成标准：文档中明确试点区域、试点线路、试点目的地清单。

### B. 稳定性与状态表达
- [ ] B01 清理核心页面的持续加载、空白页、接口失败弱提示问题。  
  范围：首页、实时线路地图、无障碍路线规划、公告页、友情链接页。
- [ ] B02 建立统一的加载态 / 空态 / 错误态 / 数据不足态文案与组件。  
  完成标准：关键页面不再只有转圈，必须给出用户可理解的下一步提示。
- [ ] B03 将“底部内容被裁切 / 双滚动条 / 抖动 / 热区点击异常”纳入自动化 UI 巡检。  
  完成标准：自动化检查中能发现并回归这些问题。

### C. 无障碍交互基线
- [ ] C01 完成键盘可达性与焦点流基线验证。  
  范围：首页、导航切换、路线列表、地图页、设置入口。
- [ ] C02 完成屏幕阅读器基线验证。  
  完成标准：至少明确并记录 NVDA / VoiceOver / TalkBack 中可验证的一组组合。
- [ ] C03 完成听障基线补齐：所有关键提示都必须有文本替代，重要事件必须有视觉提示。  
  备注：若设备支持，可补充震动提示，但不能只依赖声音。
- [ ] C04 完成大字号 / 高对比度 / 减少动态干扰的验证。  
  完成标准：核心页面在这些模式下不破版、不遮挡、不丢信息。

### D. 用户画像与推荐逻辑
- [ ] D01 至少支持两类差异化用户画像。  
  建议：`轮椅/行动不便`、`低视力`；听障先做基础可访问提示，不承诺完整路径优化。
- [ ] D02 把用户画像真正接入推荐逻辑，而不是只有展示层开关。  
  完成标准：不同画像在推荐结果、排序、风险提示上能体现差异。
- [ ] D03 给路线结果补全“推荐理由 / 风险点 / 数据来源 / 更新时间 / 置信度”。
- [ ] D04 当关键无障碍数据缺失时，系统必须降级或拒绝推荐。  
  完成标准：不能在关键节点未知时继续给出“看起来可行”的路线。

### E. 全链路无障碍建模
- [ ] E01 建模起点与终点的步行段，而不是只看公交线路本身。
- [ ] E02 建模站点上下车可达性（站台、缘石坡道、无障碍上下车条件）。
- [ ] E03 建模换乘点关键设施（电梯、坡道、台阶、通道）。
- [ ] E04 建模目的地入口级别可达性，而不是只到 POI 坐标。
- [ ] E05 在接口层返回分段结果，让前端能展示“哪一段安全、哪一段存在风险”。

### F. 数据与可信度治理
- [ ] F01 建立数据源登记表：来源、许可、更新频率、覆盖范围、可信度等级。
- [ ] F02 为关键标签建立统一置信度规则。  
  示例：人工核验 > 多源一致 > 单源推断 > 未知。
- [ ] F03 接入并整理关键无障碍数据：盲道、无障碍电梯、缘石坡道、站点无障碍等级、目的地入口可达性。
- [ ] F04 在广州建立一块“小范围人工核验试点数据”，作为真正可演示的可信样本。

### G. 反馈与人工纠错闭环
- [ ] G01 提供用户反馈入口：数据不准、路径不可达、设施异常、页面不可用。
- [ ] G02 提供最小管理/审核闭环，能把用户反馈回写为待核查数据或规则调整项。

### H. 真实用户验证
- [ ] H01 设计真实任务清单。  
  示例：去医院、去政务大厅、去地铁站、去校园建筑。
- [ ] H02 至少完成一轮目标用户或准真实用户验证。  
  完成标准：至少记录任务成功率、失败点、理解成本、是否误导用户。
- [ ] H03 将验证结论回写到本清单与 `PROJECT_CHANGELOG.md`，不是只口头确认。

### I. 最终复审
- [ ] I01 上述任务全部完成并通过对应验证。
- [ ] I02 再次正式回答：**“这个系统是不是真的能够逻辑自洽且真的适合无障碍人士？”**
- [ ] I03 如果答案是 YES：记录结论并删除本文件；如果答案是 NO：保留本文件并开启下一轮闭环。

---

## 6. 勾选记录规范
每次勾掉某项任务时，在该任务后追加简短记录，格式如下：
- 完成日期：`YYYY-MM-DD`
- 对应提交：`commit hash`
- 验证方式：命令 / 截图 / 用户测试 / 数据核验说明
- 结果：通过 / 未通过

示例：
```markdown
- [x] B02 建立统一的加载态 / 空态 / 错误态 / 数据不足态文案与组件。  
  完成日期：2026-03-10  
  对应提交：`abc1234`  
  验证方式：`UI_BASE_URL=http://127.0.0.1:8134/springbootmf383/front/ npm run ui:check` + 手工页面回归  
  结果：通过
```

---

## 7. 当前回答（锁定）
截至 **2026-03-07**，对问题：

> **“这个系统是不是真的能够逻辑自洽且真的适合无障碍人士？”**

当前回答仍然是：**NO**。

原因不是“完全没价值”，而是它目前更接近：
- 有明显无障碍意识的实验原型；
- 已经有一定工程基础和交互改造；
- 但还不足以作为目标用户可放心依赖的无障碍出行系统。

本文件存在的目的，就是把这个 **NO** 一项一项改造成可以复审的 **YES**。
