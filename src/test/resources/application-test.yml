spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:springbootmf383-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    initialization-mode: always
    schema: classpath:schema-demo.sql
    data: classpath:data-demo.sql
